# ── 모델 weights ──────────────────────────────────────
*.safetensors
*.pt
*.bin
models/*/checkpoints/

# config/tokenizer 파일은 유지
!models/*/config.json
!models/*/model_config.json
!models/*/tokenizer*.json
!models/*/special_tokens_map.json
!models/*/sentencepiece.bpe.model

# ── Python ────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# ── 로그 ──────────────────────────────────────────────
*.log

# ── 데이터 ────────────────────────────────────────────
data/
*.csv
*.jsonl
